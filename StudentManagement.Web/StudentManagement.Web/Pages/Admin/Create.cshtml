@page
@model StudentManagement.Web.Pages.Student.CreateModel
@{
    ViewData["Title"] = "Create Student";
    Layout = "_DashboardLayout";
}

<!-- Font Awesome CDN (agar _Layout me na ho to yeh add kro) -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

<style>
    .form-card {
        max-width: 600px;
        margin: 40px auto;
        background: #ffffff;
        border-radius: 16px;
        padding: 30px 35px;
        box-shadow: 0 8px 22px rgba(0,0,0,0.12);
        animation: fadeInUp 0.6s ease-in-out;
    }

    .form-title {
        font-size: 26px;
        font-weight: 700;
        margin-bottom: 25px;
        text-align: center;
        background: linear-gradient(45deg, #1e3c72, #2a5298);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .form-label {
        font-weight: 600;
        margin-bottom: 6px;
        display: block;
        color: #34495e;
    }

    .form-control {
        border-radius: 10px;
        padding: 12px 14px;
        border: 1px solid #dcdcdc;
        transition: 0.3s ease;
    }

    .form-control:focus {
        border-color: #3498db;
        box-shadow: 0 0 8px rgba(52, 152, 219, 0.4);
    }

    /* Primary Button (Save) */
    .btn-primary {
        background: linear-gradient(135deg, #1e3c72, #2a5298);
        border: none;
        padding: 12px 22px;
        border-radius: 10px;
        font-weight: 600;
        color: #fff;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        transition: all 0.3s ease;
    }

    .btn-primary:hover {
        background: linear-gradient(135deg, #2a5298, #1e3c72);
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(46, 134, 222, 0.3);
    }

    /* Secondary Button (Cancel) */
    .btn-secondary {
        padding: 12px 22px;
        border-radius: 10px;
        font-weight: 600;
        margin-left: 10px;
        background: #ecf0f1;
        color: #2c3e50;
        display: inline-flex;
        align-items: center;
        gap: 8px;
        border: none;
        transition: all 0.3s ease;
    }

    .btn-secondary:hover {
        background: #dcdcdc;
        transform: translateY(-2px);
    }

    
    }
</style>

<div class="form-card">
    <h2 class="form-title"><i class="fa-solid fa-user-plus"></i> Add New Student</h2>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger"><i class="fa-solid fa-circle-exclamation"></i> @TempData["Error"]</div>
    }
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success"><i class="fa-solid fa-circle-check"></i> @TempData["Success"]</div>
    }

    <form method="post">
        <div class="mb-3">
            <label asp-for="Student.Name" class="form-label"><i class="fa-solid fa-user"></i> Name</label>
            <input asp-for="Student.Name" class="form-control" />
            <span asp-validation-for="Student.Name" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Student.StudentCode" class="form-label"><i class="fa-solid fa-id-card"></i> Student Code</label>
            <input asp-for="Student.StudentCode" class="form-control" />
            <span asp-validation-for="Student.StudentCode" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Student.Email" class="form-label"><i class="fa-solid fa-envelope"></i> Email</label>
            <input asp-for="Student.Email" class="form-control" />
            <span asp-validation-for="Student.Email" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Student.Age" class="form-label"><i class="fa-solid fa-cake-candles"></i> Age</label>
            <input asp-for="Student.Age" type="number" class="form-control" />
            <span asp-validation-for="Student.Age" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Student.CourseId" class="form-label"><i class="fa-solid fa-book"></i> Course</label>
            <select asp-for="Student.CourseId" class="form-control" asp-items="Model.Courses"></select>
            <span asp-validation-for="Student.CourseId" class="text-danger"></span>
        </div>

        <button type="submit" class="btn-primary"><i class="fa-solid fa-floppy-disk"></i> Save</button>
        <a href="/Admin/Students" class="btn-secondary"><i class="fa-solid fa-xmark"></i> Cancel</a>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
